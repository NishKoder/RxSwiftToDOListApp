# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'TodoTestApp' do
  # Comment the next line if you don't want to use dynamic frameworks
	pod 'RxSwift', '~> 5.1'
  	pod 'RxCocoa', '~> 5.1'

  # Pods for TodoTestApp

  target 'TodoTestAppTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'TodoTestAppUITests' do
    # Pods for testing
  end

end
